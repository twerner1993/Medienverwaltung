@model IEnumerable<Medienverwaltung.Models.Interpret>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Beschreibung)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gruendung)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Beschreibung)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gruendung)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InterpretId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InterpretId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InterpretId })
        </td>
    </tr>
}

</table>
